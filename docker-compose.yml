services:

  db:

    image: postgres:16
    container_name: postgres_db

    ports:
      - "5432:5432"

    restart: always

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: veilbid
      POSTGRES_DB: veilbidDB